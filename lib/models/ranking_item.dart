///Ranking item model that holds all information associated with a Ranking card
class RankingItem {
  final String userName;
  final int profileImage;
  final int xp;

  RankingItem({this.userName, this.profileImage, this.xp});
}
